[package]
name = "simple"
version = "0.1.0"
authors = ["Inseok Lee <dlunch@gmail.com>"]
edition = "2018"

[lib]
doctest = false
crate-type = ["cdylib"]

[features]

[dependencies]
wasm-bindgen = { version = "^0.2" }
typescript-wasm-bindgen = { path = "../../.." }
